# Actor 文档迁移说明

该文档不再作为 Actor 运行时行为的事实来源。

当前基于源码提炼的 OpenSpec 规范已迁移到：

- `openspec/specs/actor/spec.md`

迁移原因：

- 文档统一使用中文
- 文档按当前仓库的 OpenSpec 目录规范放置
- 减少 `docs/` 与 `openspec/` 并存导致的双重事实来源

如果后续需要继续补充 Actor 的当前行为，请优先更新 `openspec/specs/actor/spec.md`。
